/// @description Set initial positions
// You can write your code in this editor
var initAngle = 0;
var initX = x + radius*dcos(initAngle);
var initY = y - radius*dsin(initAngle);

target = instance_create(x, y , SpecialRing);

